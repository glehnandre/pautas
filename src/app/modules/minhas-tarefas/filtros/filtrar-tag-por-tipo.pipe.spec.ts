import { FiltrarTagPorTipoPipe } from './filtrar-tag-por-tipo.pipe';

describe('FiltrarTagPorTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarTagPorTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
